package lala.example.jpa.repository;

import lala.example.jpa.domain.TravelerBestSight;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TravelerBestSightRepository extends JpaRepository<TravelerBestSight, String> {
}
